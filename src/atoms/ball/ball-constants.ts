import { COLORS } from "../../constants";

export const DEFAULT_BALL_RADIUS = 7;
export const DEFAULT_BALL_COLOR = COLORS.ORANGE;
export const DEFAULT_BALL_MOVE_SPEED = 3;
export const DEFAULT_SPEED_WITH_BOARD = 2;
